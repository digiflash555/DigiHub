// Email feature has been removed from this application.
// This stub is kept to avoid import errors if any old references remain.

const sendEmail = async ({ to, subject, htmlContent }) => {
    // Email sending disabled
};

module.exports = { sendEmail };
